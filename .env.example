# Copy to .env and set values
# API key for OpenAI-compatible endpoint (OpenAI or GitHub Models)
OPENAI_API_KEY=
# Optional: set a custom base URL (e.g. GitHub Models endpoint)
OPENAI_BASE_URL=https://models.github.ai/inference
# Port to run the proxy
PORT=3010
